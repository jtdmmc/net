<UserControl x:Class="MT.PrismApp.ModuleWorkbench.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MT.PrismApp.ModuleWorkbench.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button  Content="权限管理" FontWeight="Bold" Height="34" Width="100" Margin="10 0 0 0" Command="{Binding GotoPowerMgrCmd}"/>
            <Button  Content="绘制示例" FontWeight="Bold" Height="34" Width="100" Margin="10 0 0 0" Command="{Binding GotoDrawingExampleCmd}"/>
        </StackPanel>
    </Grid>
</UserControl>
